var caminhos = [caminho_menu, caminho_menu2, caminho_menu3, caminho_menu4];

var caminho_escolhido = caminhos[irandom(array_length(caminhos) - 1)];

path_start(caminho_escolhido, global.spd, 0,true);

image_xscale = 0.1117397;  // 50% do tamanho original na largura
image_yscale = 0.1117397;  // 50% do tamanho original na altura
